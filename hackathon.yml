name: Hack3
version: "2019" # Could also be a roman numeral or other number, will be concatenated with the hackathon name where necessary
main_website: https://hack3.co
dashboard_url: dashboard.hack3.co
logos:
  primary_path: logo.png
  nav_path: navlogo.png
emails:
  donotreply: dashboard-noreply@hack3.co
  contact: team@hack3.co
slack:
  subdomain: nonexistent
applications:
  mode: 'open'
  # options: open, waitlist, or closed. defaults to closed.
  # The applications are still not opened unless the applications feature
  # flag is enabled.
